{
  "name": "server",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "install:py": "uv sync",
    "dev": "uv run main.py",
    "start": "uv run main.py",
    "build": "echo 'No build step required for FastAPI'",
    "lint": "echo 'No linting configured'"
  }
}
